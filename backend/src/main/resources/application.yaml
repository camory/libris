spring:
  datasource:
    url: ${LIBRIS_DB_URL}
    username: ${LIBRIS_DB_USER}
    password: ${LIBRIS_DB_PASSWORD}
libris:
  sources:
    bnf-url: ${LIBRIS_BNF_URL:https://catalogue.bnf.fr/api/SRU}
    timeout: ${LIBRIS_SOURCE_TIMEOUT:5s}
management:
  endpoints:
    web:
      exposure:
        include: health,info
