# T001 ships only LibrisApplication: the D02 packages are empty until T005, so
# every rule scoped to them would fail for matching no class. Rules must still
# compile and run; they bite as soon as a class lands in a package.
archRule.failOnEmptyShould=false
