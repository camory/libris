# --- secrets: never, anywhere (D09: configuration by environment only) ---
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.jks

# --- agentic loop (run logs and local state) ---
agent/.env
agent/.proof/
agent/logs/
agent/.smoke/
.claude/settings.local.json
.claude/ralph-loop.local.md

# --- build / deps (per-module files add their own) ---
node_modules/
dist/
build/
.gradle/
*.log
.DS_Store

# --- IDE ---
.idea/
.vscode/
*.iml
